@page "/categories"
@inject Data.Categories CatagoryData

<h1>Start your ballot</h1>

<ul>
  @foreach (var cat in CatagoryData)
  {
    <li>
      <NavLink href=@($"/categories/ballot/{@cat.Slug}")>@cat.Name</NavLink>
    </li>
  }
</ul>

@code {
}